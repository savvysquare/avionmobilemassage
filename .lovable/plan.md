Update the quote text inside the "Does This Sound Like You?" cards in `src/components/site/Hero.tsx`:

- Change the `<p>` className from `text-charcoal/75 text-sm leading-relaxed font-light` to `text-charcoal/80 text-base leading-relaxed font-medium` so the copy reads slightly larger (14px → 16px) and slightly bolder (300 → 500).

No other sections, components, or styles will be touched.